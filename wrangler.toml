# Cloudflare Pages Configuration
# https://developers.cloudflare.com/pages/functions/wrangler-configuration/

name = "toonflow-vendor"
compatibility_date = "2024-01-01"

# Pages 构建输出目录（静态文件目录）
pages_build_output_dir = "."

# 环境变量（可选）
[vars]
ENVIRONMENT = "production"
